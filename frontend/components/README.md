# Reusable UI Components

This folder contains the reusable React components for the ArenaMind frontend. In a later phase, these will be organized using atomic design principles (atoms, molecules, organisms, templates) and styled with custom Tailwind CSS design tokens.
