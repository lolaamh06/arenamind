# Custom React Hooks

This folder will contain the custom hooks used across the ArenaMind frontend, such as state synchronization hooks for the live Digital Twin state and role-based notification listeners.
