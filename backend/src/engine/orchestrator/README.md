# Decision Orchestrator

The orchestrator coordinates the execution flow: aggregating context, filtering, prompting Gemini, validating results, computing confidence, saving to memory, and publishing the final structured AI Decision Brief.
