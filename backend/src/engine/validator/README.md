# Output Validator

This directory houses the safety, schema validation, and reference checks. It guarantees that the unstructured natural-language elements and structured data outputs returned by the AI are verified, safe, and syntactically correct before distribution.
