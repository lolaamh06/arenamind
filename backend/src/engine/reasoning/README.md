# Reasoning Engine (Signal Filter & Prompt Builder)

This directory is responsible for filtering raw Digital Twin data into relevant subsets based on the decision context (Signal Filter) and transforming those signals into clean, structured prompts for the Gemini model (Prompt Builder).
