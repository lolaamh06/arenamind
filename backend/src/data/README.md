# Local Data Store (JSON Files)

This folder will house the static and simulated state datasets:
- `stadium-state.json` (live context data mock)
- `scenarios.json` (hackathon test-case scenarios)
- `history.json` (archived decisions log)
